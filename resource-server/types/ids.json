{
    "user_id" : {
        "regex" : "^[a-z0-9]{24}$",
        "error" : "Invalid user ID"
    },
    "product_id" : {
        "regex" : "^[a-z0-9]{24}$",
        "error" : "Invalid product ID"
    },
    "order_id" : {
        "regex" : "^[a-z0-9]{24}$",
        "error" : "Invalid order ID"
    }
}